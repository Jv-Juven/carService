<?php

// Composer: "fzaninotto/faker": "v1.3.0"
use Faker\Factory as Faker;

class UserReadNoticeTableSeeder extends Seeder {

	public function run()
	{	
		// UserReadNotice::create([
		// 		'user_id' => $user_id,
		// 		'notice_id' => $notice_id
		// ]);
		// UserReadNotice::create([
		// 		'user_id' => $user_id,
		// 		'notice_id' => $notices[1]->notice_id
		// ]);
	}

}